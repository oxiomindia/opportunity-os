import Link from 'next/link';
export default function FeedbackTable({items}:Readonly<{items:Record<string,unknown>[]}>) { if(!items.length)return <p className="rounded-lg border border-slate-200 bg-white p-6 text-sm text-slate-500">No feedback matches this view.</p>;return <div className="overflow-x-auto rounded-xl border border-slate-200 bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-3">Submitted</th><th className="p-3">Organization</th><th className="p-3">Category / feature</th><th className="p-3">Feedback</th><th className="p-3">Status</th><th className="p-3">Priority</th></tr></thead><tbody className="divide-y divide-slate-100">{items.map((item)=>{const org=item.organizations as {name:string}|null;return <tr key={item.id as string}><td className="p-3 whitespace-nowrap">{new Date(item.submitted_at as string).toLocaleDateString()}</td><td className="p-3">{org?.name??'Unknown'}</td><td className="p-3"><span className="block">{String(item.category).replaceAll('-',' ')}</span><span className="text-xs text-slate-500">{item.feature_area as string??'—'}</span></td><td className="max-w-sm p-3"><Link href={`/admin/feedback/${item.id}`} className="font-semibold text-blue-700 hover:underline">{String(item.cons??item.pros??'Feedback').slice(0,100)}</Link>{Boolean(item.urgent_review)&&<span className="ml-2 rounded bg-red-100 px-2 py-1 text-xs font-semibold text-red-800">Urgent</span>}</td><td className="p-3 capitalize">{String(item.status).replaceAll('-',' ')}</td><td className="p-3"><span className="capitalize">{String(item.human_priority)}</span><span className="block text-xs text-slate-500">Suggested: {String(item.engine_suggested_priority)}</span></td></tr>})}</tbody></table></div>}
