import { listAdminFeedback } from '../../../../lib/feedback/admin'; import FeedbackTable from '../FeedbackTable';
export default async function Inbox({searchParams}:Readonly<{searchParams:Promise<Record<string,string|undefined>>}>){const params=await searchParams;const items=await listAdminFeedback({search:params.q,status:params.status,category:params.category,before:params.before});return <div className="space-y-5"><h1 className="text-2xl font-semibold">Feedback inbox</h1><form className="grid gap-3 rounded-lg border border-slate-200 bg-white p-4 sm:grid-cols-4"><input name="q" defaultValue={params.q} placeholder="Search feedback" className="rounded border border-slate-300 px-3 py-2"/><select name="status" defaultValue={params.status??''} className="rounded border border-slate-300 px-3 py-2"><option value="">All statuses</option><option value="new">New</option><option value="under-review">Under review</option><option value="planned">Planned</option><option value="resolved">Resolved</option></select><input name="category" defaultValue={params.category} placeholder="Category" className="rounded border border-slate-300 px-3 py-2"/><button className="rounded bg-blue-600 px-4 py-2 font-semibold text-white">Filter</button></form><FeedbackTable items={items as unknown as Record<string,unknown>[]}/></div>}
