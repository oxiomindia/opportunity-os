'use client';import{useActionState}from'react';import{resetPassword}from'./actions';export default function ResetForm(){const[s,a,p]=useActionState(resetPassword,{});return <form action={a} className="mt-6 space-y-4">{['password','confirmPassword'].map((n)=><div key={n}><label htmlFor={n} className="text-sm font-semibold">{n==='password'?'New Password':'Confirm Password'}</label><input id={n} name={n} type="password" required className="mt-2 h-11 w-full rounded-lg border px-3"/></div>)}{s.error&&<p role="alert" className="text-sm text-red-700">{s.error}</p>}{s.success&&<p role="status" className="text-sm text-green-700">{s.success}</p>}<button disabled={p} className="h-11 w-full rounded-lg bg-blue-600 font-semibold text-white">Update password</button></form>}
