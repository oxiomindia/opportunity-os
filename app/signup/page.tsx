import Link from 'next/link';import { redirect } from 'next/navigation';import { getAuthenticatedUser } from '../../lib/auth/dal';import SignupForm from './SignupForm';
export default async function SignupPage(){if(await getAuthenticatedUser())redirect('/dashboard');return <main className="flex min-h-screen items-center justify-center bg-slate-50 px-4 py-12"><section className="w-full max-w-lg rounded-2xl border border-slate-200 bg-white p-8"><h1 className="text-2xl font-semibold">Create your Oxiom account</h1><p className="mt-2 text-sm text-slate-600">Verify your work email to create your organization workspace.</p><SignupForm/><p className="mt-6 text-center text-sm">Already registered? <Link className="font-semibold text-blue-700" href="/login">Sign in</Link></p></section></main>}
