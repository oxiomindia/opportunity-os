import { Metadata } from 'next';
import Link from 'next/link';
import { buildMetadata } from '../../../../lib/seo/metadata';

export const metadata: Metadata = buildMetadata({ path: '/docs/api', title: 'API Documentation | Oxiom Invoice Processing', description: 'Oxiom Invoice Processing API documentation. REST API endpoints, authentication, request/response formats, examples.' });

const endpoints = [
  ['GET /invoices', 'List invoices with filtering by status, supplier, amount, date range, and workflow ownership.'],
  ['POST /invoices', 'Create or ingest a new invoice record with source metadata and document references.'],
  ['GET /invoices/{id}', 'Retrieve invoice header data, status, workflow history, and related document metadata.'],
  ['PATCH /invoices/{id}/status', 'Update invoice workflow status when the calling integration has appropriate permissions.'],
  ['GET /vendors', 'Return vendor master data available to the authenticated account or integration scope.'],
  ['GET /reports/summary', 'Fetch aggregate reporting metrics for throughput, aging, and exception activity.'],
];

const errors = [
  ['400', 'Bad Request', 'The request payload is malformed, incomplete, or violates validation rules.'],
  ['401', 'Unauthorized', 'The API key is missing, invalid, expired, or not authorized for the request scope.'],
  ['403', 'Forbidden', 'The integration is authenticated but lacks permission for the requested resource or action.'],
  ['404', 'Not Found', 'The requested resource does not exist or is out of scope.'],
  ['429', 'Rate Limited', 'The integration exceeded the current request threshold.'],
  ['500', 'Server Error', 'An unexpected platform issue occurred.'],
];

export default function ApiDocumentationPage() {
  return <div className="min-h-screen bg-white"><div className="border-b border-slate-100 bg-slate-50 py-12 sm:py-16"><div className="mx-auto max-w-7xl px-6 sm:px-8 lg:px-10"><h1 className="mb-4 text-4xl font-semibold tracking-tight text-slate-950 sm:text-5xl">API Documentation</h1><p className="max-w-3xl text-lg text-slate-600">Integrate with Oxiom Invoice Processing using secure REST APIs for invoice ingestion, retrieval, workflow updates, vendor data, and reporting.</p></div></div><div className="mx-auto max-w-7xl px-6 py-16 sm:px-8 sm:py-20 lg:px-10"><section className="mb-16 grid gap-6 md:grid-cols-3"><div className="rounded-lg border border-slate-200 bg-white p-6"><h2 className="mb-3 text-2xl font-semibold text-slate-950">Authentication</h2><p className="text-sm leading-6 text-slate-600">API access uses scoped API keys issued by an Oxiom administrator. Send the key in the Authorization header as a bearer token and rotate keys according to your security policy.</p></div><div className="rounded-lg border border-slate-200 bg-white p-6"><h2 className="mb-3 text-2xl font-semibold text-slate-950">Base URL</h2><p className="font-mono text-sm text-blue-700">https://api.oxiom.in/v1</p><p className="mt-3 text-sm leading-6 text-slate-600">Use environment-specific credentials and network controls when connecting from production integrations.</p></div><div className="rounded-lg border border-slate-200 bg-white p-6"><h2 className="mb-3 text-2xl font-semibold text-slate-950">Rate limits</h2><p className="text-sm leading-6 text-slate-600">Integrations should expect request-rate controls and respect retry headers during peak processing windows.</p></div></section><section className="mb-16"><h2 className="mb-6 text-3xl font-semibold text-slate-950">Endpoints overview</h2><div className="grid gap-6 md:grid-cols-2">{endpoints.map(([route, description]) => (<div key={route} className="rounded-lg border border-slate-200 bg-white p-6"><h3 className="mb-3 font-mono text-sm font-semibold text-blue-700">{route}</h3><p className="text-sm leading-6 text-slate-600">{description}</p></div>))}</div></section><section className="mb-16 grid gap-10 lg:grid-cols-2"><div><h2 className="mb-4 text-3xl font-semibold text-slate-950">Request/response formats</h2><p className="text-base leading-7 text-slate-600">Requests and responses use JSON. Invoice objects typically include document identifiers, vendor references, totals, status, timestamps, workflow ownership, and supporting metadata required for downstream processing or reporting.</p></div><div><h2 className="mb-4 text-3xl font-semibold text-slate-950">Webhooks and SDKs</h2><p className="text-base leading-7 text-slate-600">Oxiom can publish event notifications for invoice status changes. Subscribers should verify signatures, log deliveries securely, and design handlers to process duplicate deliveries safely. Integrations are commonly implemented with existing enterprise HTTP clients and middleware.</p></div></section><section className="mb-16"><h2 className="mb-6 text-3xl font-semibold text-slate-950">Error codes</h2><div className="overflow-x-auto rounded-lg border border-slate-200 bg-white"><table className="min-w-full divide-y divide-slate-200 text-left text-sm text-slate-600"><thead className="bg-slate-50 text-slate-950"><tr><th className="px-4 py-3 font-semibold">HTTP</th><th className="px-4 py-3 font-semibold">Meaning</th><th className="px-4 py-3 font-semibold">Guidance</th></tr></thead><tbody className="divide-y divide-slate-200">{errors.map(([code, meaning, guidance]) => (<tr key={code}><td className="px-4 py-3 font-medium text-slate-950">{code}</td><td className="px-4 py-3">{meaning}</td><td className="px-4 py-3">{guidance}</td></tr>))}</tbody></table></div></section></div><div className="border-t border-slate-100 bg-slate-50 py-16 sm:py-20"><div className="mx-auto max-w-5xl px-6 sm:px-8 lg:px-10"><div className="grid gap-6 md:grid-cols-3"><Link href="/docs/implementation" className="rounded-lg border border-slate-200 bg-white p-6 hover:border-blue-300 hover:bg-blue-50"><h3 className="mb-2 text-lg font-semibold text-slate-950">Implementation guide</h3></Link><Link href="/security" className="rounded-lg border border-slate-200 bg-white p-6 hover:border-blue-300 hover:bg-blue-50"><h3 className="mb-2 text-lg font-semibold text-slate-950">Security overview</h3></Link><Link href="/support" className="rounded-lg border border-slate-200 bg-white p-6 hover:border-blue-300 hover:bg-blue-50"><h3 className="mb-2 text-lg font-semibold text-slate-950">Support</h3></Link></div></div></div></div>;
}
